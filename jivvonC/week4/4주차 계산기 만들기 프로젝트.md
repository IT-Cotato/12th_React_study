# 9. 계산기 만들기 프로젝트

### 계산기 리팩토링

```tsx
export default function Calculator () {
  const handleClear = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    console.log(e.currentTarget.value);
  };
    const handleOperator = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    console.log(e.currentTarget.value);
  };
    const handleNum = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    console.log(e.currentTarget.value);
  };
      const handleDot = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    console.log(e.currentTarget.value);
  };

  const buttonConfigs = [
    {value: "C", className:'calc-clear', onClick: handleClear},
    {value: "/", className:'calc-operator', onClick: handleOperator},
    {value: "1", className:'calc-num', onClick: handleNum},
    {value: "2", className:'calc-num', onClick: handleNum},
    {value: "3", className:'calc-num', onClick: handleNum},
    {value: "*", className:'calc-operator', onClick: handleOperator},
    {value: "4", className:'calc-num', onClick: handleNum},
    {value: "5", className:'calc-num', onClick: handleNum},
    {value: "6", className:'calc-num', onClick: handleNum},
    {value: "+", className:'calc-operator', onClick: handleOperator},
    {value: "7", className:'calc-num', onClick: handleNum},
    {value: "8", className:'calc-num', onClick: handleNum},
    {value: "9", className:'calc-num', onClick: handleNum},
    {value: "-", className:'calc-operator', onClick: handleOperator},
    {value: ".", className:'calc-dot', onClick: handleDot},
    {value: "0", className:'calc-num', onClick: handleNum},
    {value: "=", className:'calc-result', onClick: handleOperator},
  ]
  return (
    <>
    <div className="bg-[#1f1f1f] flex items-center justify-center h-screen">
    <article className="w-[282px] border border-[#333] bg-[#ccc] p-1">
      <form className="grid grid-cols-[repeat(4,65px)] auto-rows-[65px] gap-1" name="forms">
        <input type="text" className="calc-input" name="output" readOnly onClick={(e) => handleClear(e)}/>
        {buttonConfigs.map((button) => (
          <CalculatorButton key={button.value} {...button}/>
          ))}
      </form>
    </article>
    </div>
    </>
  );
}
```

데이터(내용) 부분을 배열 객체로 객체화하고 map 함수를 이용한 반복 렌더링하기

### 계산기 타입 빼기

타입 추론을 이용하는 방법 vs 직접 타입을 작성하는 방법

```tsx
//types/propd.d.ts
type ButtonConfigs = {
    value: string;
    className: string;
    onClick: (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => void;
}

//Calculator.tsx
const buttonConfigs : ButtonConfigs[] = [
    {value: "C", className:'calc-clear', onClick: handleClear},
    {value: "/", className:'calc-operator', onClick: handleOperator},
    {value: "1", className:'calc-num', onClick: handleNum},
    {value: "2", className:'calc-num', onClick: handleNum},
    {value: "3", className:'calc-num', onClick: handleNum},
    {value: "*", className:'calc-operator', onClick: handleOperator},
    {value: "4", className:'calc-num', onClick: handleNum},
    {value: "5", className:'calc-num', onClick: handleNum},
    {value: "6", className:'calc-num', onClick: handleNum},
    {value: "+", className:'calc-operator', onClick: handleOperator},
    {value: "7", className:'calc-num', onClick: handleNum},
    {value: "8", className:'calc-num', onClick: handleNum},
    {value: "9", className:'calc-num', onClick: handleNum},
    {value: "-", className:'calc-operator', onClick: handleOperator},
    {value: ".", className:'calc-dot', onClick: handleDot},
    {value: "0", className:'calc-num', onClick: handleNum},
    {value: "=", className:'calc-result', onClick: handleOperator},
  ]
  
```

이렇게 직접 타입을 작성하면 타입을 알아보기 쉬워진다는 장점 + props로 데이터를 전달할 때, 일일히 적어줄 필요 없고 ButtonConfigs라는 타입을 전달해준다고만 하면 됨.

타입추론에 의존해도 괜찮지만, 상황에 맞게 타입을 지정해서 잘 쓴다면 이러한 이점들도 있다~

### 계산기 기능 구현-숫자 입력 받기

```tsx
    //0 - 9까지 클릭했을 때 실행되는 함수
    const handleNum = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    console.log(e.currentTarget.value);
    const value = e.currentTarget.value; //문자열

    //isNewNumber가 true면 value를 저장, 아니면 현재 입력되어있는 값에 value를 덧셈
    setCalculatorState(calculatorState => ({
      ...calculatorState,
      currentNumber : calculatorState.isNewNumber
        ? value
        : calculatorState.currentNumber + value,

      isNewNumber: false,

    }));
    };
```

### 계산기 기능 구현-연산자

```tsx
// "+", "-", "*", "/", "=" 클릭되었을 때 실행되는 함수
    const handleOperator = (e: React.MouseEvent<HTMLInputElement, MouseEvent>) => {
    const operator = e.currentTarget.value;
    setCalculatorState((calculatorState) => {

      if(calculatorState.currentNumber === "" && operator)
        return calculatorState;

      const current = parseFloat(calculatorState.currentNumber);

      //둘 다 있으면 계산해라
      if(calculatorState.previousNumber && calculatorState.operation) {
        const prev = parseFloat(calculatorState.previousNumber);
        const result = performCalculation(prev, current, calculatorState.operation)
        //결과가 보고싶은 거라면 결과 뽑아주고
        return operator === "=" ? {
          currentNumber: result.toString(), 
          previousNumber: "",
          operation: null,
          isNewNumber: true
        }: {
        //아니라면 계속해라
        currentNumber: "",
        previousNumber: result.toString(),
        operation: operator,
        isNewNumber: true,
        };
      }
      // "="를 누르는 경우 > 새 값 받을 준비해라, 비워라
      // 그 외의 연산자 > 연산자 이전까지 previousNumber에 저장하고 다시 새 값 받을 준비해라, 비워라
      else if (operator === "=") {
      return {...calculatorState, isNewNumber: true}; //나머지 상태값 그대로, isNewNumber만 true로 바꿔라 
      }

      else{
      return {
          currentNumber: "",
          previousNumber: current.toString(),
          operation: operator,
          isNewNumber: true,
      };
      }
    });
  };
```

연산자별로 다른 상황 > 다양한 상태의 조합

콘솔에서 어떻게 상태가 바뀌는지 잘 확인하면서 진행

### 계산기 기능 구현- 소수점

```tsx
// . 클릭했을 때 실행되는 함수
    const handleDot = () => {
    setCalculatorState(calculatorState => {
      if(calculatorState.currentNumber.includes(".")) return calculatorState;
      return {
        ...calculatorState,
        currentNumber: calculatorState.currentNumber + ".",
        isNewNumber: false,
      }
    })
  };
```

똑같이 isNumber: false로 소숫점만 추가하고 계속 입력값 받으라는 것

함수 여러개 복잡하게 나올 때는 주석 필수!

### 계산기 기능 구현-클리어

```tsx
export const initialData : CalculatorState = {
  currentNumber: "0", //현재 입력/표시되는 숫자
  previousNumber: "", //이전에 입력된 숫자
  operation: null, //현재 선택된 연산자 ("+", "-", "/") 나중에 연산자 문자열로 들어와서 null != 문자열 문제를 해결해주기 위해 명확한 타입 작성
  isNewNumber: true, //새로운 숫자 입력 여부
};

  // 'C'를 클릭했을 때 함수
  const handleClear = () => {
   setCalculatorState(initialData);
  };
```

반복적으로 쓰이는 데이터도 가독성을 위해 따로 빼는 것 가능

사소한 차이지만 쌓이면 코드 가독성에서 큰 차이가 된다!

### 계산기 기능 구현- 버그 및 마무리

연산자 연속으로 2번 입력시 Nan 발생하는 문제

```tsx
      if(calculatorState.currentNumber === "" && operator)
        return calculatorState;
```

컴포넌트 안에 작성하는 함수 vs 컴포넌트 밖에 작성하는 함수

리액트 관련 기능이 있다 > 컴포넌트 안에 작성 (ex. 상태관리함수, 이벤트 핸들러 등)

없으면 그냥 컴포넌트 밖에 작성해도 상관없음.

컴포넌트 export 위에 다른 함수가 있는게 거슬린다면

`src/utils/calculatorUtils.ts` 로 따로 빼고 import 해오기
